# API Base URL configuration
# Local development: Leave empty to use Vite proxy (defaults to /api/v1)
# Production: Set to full backend URL, e.g., https://dayform-api.onrender.com/api/v1
VITE_API_BASE_URL=
