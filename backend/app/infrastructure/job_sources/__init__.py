"""Job source adapters.

- `adzuna.py` — implemented in Milestone 3.

Greenhouse/Lever adapters follow the same `JobSourceAdapter` protocol
(`app/application/jobs/ports.py`) — added in a later milestone.
"""
