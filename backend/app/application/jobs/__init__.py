"""Job discovery use cases: ingesting postings from external sources and
listing them. Added in Milestone 3.

This package wasn't in the original Milestone 0 folder sketch (which named
scoring/resumes/cover_letters/applications/automation) — job ingestion is
a distinct use case from application *tracking* (`application/applications/`)
and didn't have a home. Adding it here follows the same one-subpackage-
per-use-case pattern already established, rather than overloading an
existing package with an unrelated responsibility.
"""
