"""Application layer: use-case services that orchestrate domain logic and
infrastructure ports. No framework imports (no FastAPI, no SQLAlchemy
sessions directly) — only interfaces/ports the infrastructure layer implements."""
